import React from 'react';
import Activate from '../activate.png';

const Header = () => {
  return (
    <header>
      <div className="logo">
        <img src={Activate} />
      </div>
    </header>
  );
}

export default Header;
